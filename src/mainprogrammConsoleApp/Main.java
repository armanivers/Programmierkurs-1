package mainprogrammConsoleApp;
public class Main {
	public static void main(String[] args) {

		//Menu starten
		Menu m = new Menu();
		m.start();
		
		
		/*
		 * Testausgaben fur hashCode und equals
		audio1.druckeDaten();
		bild1.druckeDaten();
		
		System.out.println(audio1.getTitel() +" ist " +audio1.alter());
		
		if(audio1.equals(audio2)) {
			System.out.println("Die Objekte mit ID " +audio1.getId() +" und " +audio2.getId() +" sind (fachlich) gleich");
			System.out.println("Die gleichen Objekte haben den Hashcode " +audio1.hashCode() +" und " +audio2.hashCode());
		}
		else {
			System.out.println("Die Objekte mit ID " +audio1.getId() +" und " +audio2.getId() +" sind (fachlich) nicht gleich");
		}
		
		if(bild1.equals(bild2)) {
			System.out.println("Die Objekte mit ID " +audio1.getId() +" und " +audio2.getId() +" sind (fachlich) gleich");
			System.out.println("Die gleichen Objekte haben den Hashcode " +audio1.hashCode() +" und " +audio2.hashCode());
		}
		else {
			System.out.println("Die Objekte mit ID " +audio1.getId() +" und " +audio2.getId() +" sind (fachlich) nicht gleich");
		}
		*/
		
	}
}
